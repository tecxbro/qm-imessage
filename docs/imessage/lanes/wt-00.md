# WT00 corrective foundation handoff

## Identity

- Repository: `https://github.com/tecxbro/qm-imessage.git`
- Repair worktree: `/Users/darshan/Documents/ChatGPT/qm-imessage/worktrees/wt-integration`
- Repair branch: `imessage/integration`
- Original QM baseline: `0e3f9b9739f5ac695aa02672860ead64da88105e`
- Reviewed foundation commit: `1f3df849a29e03a415c6626c2865c2d1a59b76f9`
- Preserved original tag: `qm-imessage-f0`
- Planned corrective tag: `qm-imessage-f0-r1`

The repair reuses the clean registered integration worktree. It does not replace the repository, repurpose main or foundation, renumber lanes, or implement feature-lane production files.

## Corrected foundation

- Restored the original WT01-WT25 waves, branches, worktree paths, production paths, tests, lane documents, and integration reservations.
- Replaced message-shaped generic intake with discriminated message, edit, unsend, reaction, read, delivery, poll, group, membership, lifecycle, and unknown events.
- Separated provider event identity from scoped message and part identity.
- Made project identity optional before selection and added an allowlisted dashboard projection that cannot return private credential fields.
- Replaced unrestricted operation input bags with exact TypeScript payload maps, pinned effect identifiers, and runtime validation for JSON-safe plain values.
- Added recoverable inbox payloads, structured tenant keys, fenced claims, atomic receipt/cursor checkpoints, durable ordered stream sessions, multipart-part conflict detection, logical operation deduplication, per-part delivery state, exact provider receipts, explicit logical-part confirmations, and evidence-based reconciliation contracts.
- Kept provider clients named and capability-limited; no generic SDK method executor, PostgreSQL implementation, route, worker, or production provider was added.
- Separated strict lane verification from captured-SHA integration verification and exercised failure propagation in temporary repositories and worktrees.
- Replaced destructive HTML stripping with structured parsing that preserves escaped generics, JSX, operators, multiline code, syntax spans, ordered and nested lists, and document structure.
- Added provenance, hash, origin, resolved-host, coverage, and safe-path checks without rewriting the valid official Markdown snapshots.

## Source and declaration evidence

The committed official corpus remains 54 source pages plus the discovery index. All source pages retain their original bytes and `official-markdown` provenance. Validation recomputes body hashes, compares discovery coverage, matches embedded provenance to the lock, and rejects unsafe or contradictory entries.

The Photon package remains pinned to `spectrum-ts` 12.8.0, `@photon-ai/advanced-imessage` 2.1.0, and `@photon-ai/cli` 2.2.0. Type-only declaration checks independently cover Spectrum void edit, Advanced iMessage void unsend, concrete text-send and edit messages, poll creation and mutation `Poll` results, restart-safe `MiniAppMessageResult` app-card sessions, and `Chat` group-mutation results. Full provider-operation return-shape implications and compatibility uncertainties are recorded in `docs/imessage/contracts.md`.

## Evidence boundary

This is foundation and tooling evidence only. PostgreSQL adapters, actual CLI login, Photon setup, Spectrum connection, provider operations, integration composition, live delivery/read, and physical-device rendering remain unimplemented or unverified in their owning lanes. No live message, provisioning, secret rotation, deployment, push, or main merge occurred.

Final corrective commit, review, tag, and worktree results remain pending until verification completes; they will be recorded in `docs/imessage/integration/foundation-repair.md` and the post-freeze checkpoint.
