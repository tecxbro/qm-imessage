# R10: line-scoped Photon runtime credentials

## Repair identity

R10 is limited to resolving credentials for one configured Photon line. Its registered checkout is `/Users/darshan/Documents/ChatGPT/qmimessage/worktrees/cp1-r10` on `cp1/r10`. The repair base is `fe92c5b1a4aeddf4b8fdf893c37f7ccbeec81cb3`; the imported candidate was `8a853f6a425a6667644b5cb2805370ae10d5ddc1`.

The reviewed integration input remains `integration-1` at `4b1ce5a8326979cb8cf59eb966df4906a901dd38`. R10 owns only:

- `plugins/photon/src/provider/credentials.ts`
- `plugins/photon/test/credentials.test.ts`
- `docs/imessage/repairs/r10.md`

The canonical shared contract is `docs/imessage/repairs/contract.md:277-310`, and the ownership entry is `docs/imessage/repairs/ownership.json:149-157`. The connection manager and line owner remain R14-owned.

## Verified provider boundary

The lockfile pins `spectrum-ts` 12.8.0 and `@photon-ai/advanced-imessage` 2.1.0. The installed public Spectrum declarations expose `cloud.issueImessageTokens(projectId, projectSecret): Promise<TokenData>`. A dedicated response contains `auth`, `numbers`, `expiresIn`, and `type: "dedicated"`. The installed provider maps `auth[instanceId]` to the line bearer token and `numbers[instanceId]` to its phone.

The frozen official connection guide distinguishes automatic project discovery from explicit line clients. Automatic discovery accepts a project ID and project secret and renews internally. The existing QM constructor instead uses an explicit `{ address, token, phone }` client; the guide states that explicit-client tokens require external renewal. It also documents the dedicated address form `line-1.imsg.photon.codes:443`. R10 does not invent an HTTP endpoint or pass project credentials into the explicit client's token field.

Advanced 2.1.0 accepts an explicit address and bearer token but exposes no verified project-secret acquisition method. Advanced acquisition therefore returns `PHOTON_CREDENTIAL_ACQUISITION_UNSUPPORTED`. Shared Spectrum credentials expose no authoritative one-line mapping and return `PHOTON_CREDENTIAL_LINE_SCOPE_UNAVAILABLE`.

## Resolution behavior

`createSpectrumCloudLineCredentialAcquirer` accepts an exact installation/project-scoped private project-secret source. It passes that secret only to the public token issuer, accepts only a dedicated response, selects only the own-property entry for the trusted line ID, verifies the returned phone, derives the documented dedicated address from the trusted line ID, and converts the provider TTL to a canonical absolute expiry. A missing line token fails closed; the project secret is never used as fallback bearer authority.

`resolveLineCredential` snapshots the trusted line before asynchronous work and verifies provider, installation, project, line, phone, address, bearer token, canonical expiry, and remaining lifetime. The acquisition start is the conservative TTL origin, and a second clock read accounts for acquisition delay. Invalid or backwards clocks, malformed/prototype-inherited results, unknown discriminants, untrusted addresses, expired or near-expiry credentials, and thrown source/provider errors return fixed safe codes without thrown text or stale credentials.

`createPhotonLineCredentialResolver` imports and implements the coordinator-owned `PhotonLineCredentialResolver`, `PhotonLineCredentialResolution`, and `PhotonProviderMode`; it does not redeclare them. It rejects wrong installation, line, mode, or noncanonical request time before reading private credentials or invoking the issuer. An available dedicated Spectrum result carries the exact request scope, bearer token, canonical expiry, `persisted-line-assignment` provenance, and `external` renewal. Near-expiry results use the canonical `expired` branch.

No management-login token is accepted by any R10 API. Project secrets and line bearer tokens have distinct source/result types and dataflow. The module has no logging path. Available bearer material remains private process data; unavailable and expired projections contain safe codes only.

## Verification evidence

Verification uses Node 24.18.0 and npm 11.10.0 to satisfy the repository engines. `npm ci --prefix plugins/photon` restored only the pinned plugin dependencies and did not change tracked files.

Focused verification covers exact issuer inputs/output mapping, every scope mismatch, pre-secret scope rejection, project-secret non-fallback, malformed provider/private-source responses, dedicated versus shared modes, Advanced unavailability, canonical TTL/expiry and renewal margin, delayed acquisition, refresh failure without stale fallback, safe error projection, asynchronous scope mutation, and connection replacement without duplicate intake.

The credential file reports 35 passing tests. The credential plus affected provider files report 61 passing tests, the complete plugin reports 145, and the repository ownership file reports 8. Plugin and root TypeScript, targeted ESLint/oxlint, Prettier, and diff-whitespace checks pass. The project-secret non-fallback assertion was also exercised red/green by temporarily adding the prohibited fallback, observing the focused failure, restoring the implementation, and rerunning the final gate.

## Integration requests and evidence boundary

R15 must construct `HostPrivateProjectSecretSource` from the encrypted private installation record and supply the exact persisted `ProviderLine`. R14 must revalidate and consume the same line, address, phone, token, and expiry under its durable line-owner fence, schedule replacement before the renewal margin, and keep the old connection only when refresh fails before expiry. Final composition must never serialize a ready/private credential result into logs or public status.

The canonical public result omits address, phone, and project scope, while the current manager needs the private `ScopedLineCredential`. Composition must keep that richer value inside the Photon process or make a coordinator-owned contract adjustment; it must not reconstruct authority from message or card input.

No live project secret, management token, line token, provider authentication, activation, delivery, or device observation is part of this repair. No manager edit, push, merge, tag, release, or deployment is performed.
